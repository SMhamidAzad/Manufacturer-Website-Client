import React from 'react';

const NotFound = () => {
    return (
        <div>
            <h1 className="text-red-700 text-center">Your searching result are not found</h1>
        </div>
    );
};

export default NotFound;